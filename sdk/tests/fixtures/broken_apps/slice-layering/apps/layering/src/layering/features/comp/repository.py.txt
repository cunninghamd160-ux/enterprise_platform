from collections.abc import Sequence

from sqlalchemy import RowMapping, text

from insights_platform.data import get_engine


async def list_comp() -> Sequence[RowMapping]:
    async with get_engine("warehouse").connect() as conn:
        result = await conn.execute(text("select employee_id, team from compensation"))
        return result.mappings().all()
